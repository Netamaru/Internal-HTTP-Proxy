# Squid configuration file

# Define the port Squid listens on
http_port 3128

# Access control lists (ACLs)
acl all src all   # Allow access from any IP address

# Allow HTTP access from all IP addresses
http_access allow all

# Disable caching
cache deny all
